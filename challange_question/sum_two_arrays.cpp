#include <bits/stdc++.h>
using namespace std;
int main()
{
    int j = 0;
    int arr_1[] = {1, 2, 3, 4, 5};
    int arr_2[] = {1, 2, 3, 4, 5};
    int size = sizeof(arr_1) / sizeof(arr_1[0]);
    int size_2 = sizeof(arr_2) / sizeof(arr_2[0]);

    return 0;
}