#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> sahil;
    sahil.push_back(1);
    sahil.push_back(2);
    sahil.push_back(3);
    sahil.push_back(4);
    sahil.push_back(5);
    // cout << binary_search(sahil.begin(), sahil.end(), 5);
    string a = "sahil";
    reverse(a.begin(), a.end());
    cout << a << endl;

    return 0;
}