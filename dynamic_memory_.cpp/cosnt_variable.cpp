#include <bits/stdc++.h>
using namespace std;
int main()
{
    const int a = 120;
    // int &d = a;  since the path of the variable is const and
    // and cannot be changed therefore it is not possible to create
    // another variable having the read and write acess

    // of the const variable

    return 0;
}
 