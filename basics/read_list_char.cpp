#include <bits/stdc++.h>
using namespace std;
int main()
{
    char ch;
    cout << " enter the char " << endl;
    ch = cin.get();
    while (ch != '.')
    {
        cout << ch << "." << endl;
    }

    return 0;
}