#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    vector<int> bfsofGraph(int v, vector<int> adj[])
    {
        vector<int> bfs;
        vector<int> visited(v + 1, 0);

        return bfs;
    }
};
int main()
{

    return 0;
}